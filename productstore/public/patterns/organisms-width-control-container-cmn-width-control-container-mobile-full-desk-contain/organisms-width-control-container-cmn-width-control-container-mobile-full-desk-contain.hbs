<div class="o-widthControl mx-auto {{#if IsNarrowWidth}} o-widthControl__narrow {{/if}}{{#else}} w-full max-w-full {{/else}}{{#if IsContainedAtMobile}}to-md:px-10{{/if}} {{#if IsContainedAtDesktop}}md:max-w-contain{{/if}}">
  <!-- Here any component or rich text could be added  -->
  <!-- DEMO The following content is only for demo purposes -->
  {{#if ContentDynamic}}
    {{#each ContentDynamic}}
      {{> (lookup . 'DynamicPartial')}}
    {{/each}}
  {{else}}
    {{{ContentStatic}}}
  {{/if}}
  <!-- END DEMO -->
</div>