<m-mkt-text-image-banner header="{{ Header }}" header-classes="{{ HeaderClasses }}" subheader="{{ Subheader }}" subheader-classes="{{ SubheaderClasses }}" :image-desktop='{{toJson ImageDesktop}}' image-desktop-classes="{{ ImageDesktopClasses }}" :image-mobile='{{toJson ImageMobile}}' image-mobile-classes="{{ ImageMobileClasses }}">
</m-mkt-text-image-banner>