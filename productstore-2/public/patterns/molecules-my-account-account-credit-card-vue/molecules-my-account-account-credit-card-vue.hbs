<credit-card-info card-name="{{ CardName }}" card-ending="{{ CardEnding.Number }}" card-ending-aria-label="{{ CardEnding.AriaLabel }}" card-expiration="{{ Expires.Date }}" card-expiration-label="{{ Expires.Label }}" :card-expiration-near="{{ Expires.ExpirationNear }}" card-expiration-aria-label="{{ Expires.AriaLabel }}" :card-brand-image-src='{{{toJson CardLogo}}}' card-brand-alt="{{ CardLogo.Alt }}">
</credit-card-info>