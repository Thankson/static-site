<h1>
  <center>FE ONLY: This carousel relies on pagination! Read MD file for more details.</center>
</h1>
{{#with MSC}}
  <!-- START 16553 - Monthly Special Carousel -->
  <monthly-special-carousel active="{{Active}}" button-next-slide="{{ButtonNextSlide}}" button-prev-slide="{{ButtonPrevSlide}}" by="{{By}}" carousel-address="{{Carousel.Address}}" carousel-id="{{Carousel.Id}}" concentrated-label={{Concentrated}} diluted-label={{Diluted}} earn="{{Earn}}" for-string="{{ForString}}" impressions-title="{{Localization.Data.ImpressionsTitle}}" loading="{{Loading}}" not-active="{{NotActive}}" of="{{Of}}" purchase="{{Purchase}}" slide="{{Slide}}" cta="{{Cta}}" sold-out="{{SoldOut}}" save="{{Save}}">

  </monthly-special-carousel>
  <!-- END START 16553 - Monthly Special Carousel -->
{{/with}}