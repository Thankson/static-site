<o-address-search alert-label="{{AlertLabel}}" cancel-label="{{CancelLabel}}" confirm-label="{{ConfirmLabel}}" heading-label="{{Heading}}" info-label="{{InfoLabel}}" input-placeholder="{{InputPlaceholder}}" search-label="{{SearchLabel}}" subheading-label="{{Subheading}}">
</o-address-search>