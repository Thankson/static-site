<a-pay-label account-number-preview="{{Payment.LastFourDigitsOfAccount}}" display-name="{{Payment.PaymethodNameDisplay}}" expiration-date="{{Payment.ExpirationDisplay}}" expires-label="Expires" expired-label="Expired" :expires-soon="{{Payment.ExpiresSoon}}" :is-expired="{{Payment.Expired}}" :image='{{toJson Payment.PayMethodImage}}' type="{{Payment.PayMethodType}}" />